apiVersion: v1
kind: ConfigMap
metadata:
  name: kube-app-config
  namespace: kube-app-ns
data:
  application.yml: |
    server:
      port: 8080
    management:
      endpoints:
        web:
          exposure:
            include: health,info,metrics
      endpoint:
        health:
          show-details: always
